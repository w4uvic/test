# Astro Starter

This is a minimal [Astro](https://astro.build/) project scaffolded offline.

## Setup

Run `npm install` to install dependencies. (Requires internet access to fetch `astro`.)

## Development

Use `npm run dev` to start the development server.

## Build

Use `npm run build` to generate the production site in the `dist/` directory.

